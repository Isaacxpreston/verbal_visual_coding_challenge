const styles = {
  linkText: {
    textAlign: "center",
    fontSize: "0.85em",
    padding: "2px",
    transition: "transform 0.1s",
    ":hover": {
      transform: "scale(1.25, 1.25)"
    }
  }
}

export default styles